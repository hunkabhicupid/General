#include <string>
#include <vector>
#include <map>
using namespace std;

int main()
{
    string a =  "abhishek";
    int b = 0;
    map<string, int> x;
    x.insert(pair<string, int>(a,b));
    vector<string> v;
    v.push_back(a);
    return -1;
}


