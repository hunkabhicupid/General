#include <stdio.h>

int main ()
{
    unsigned int M,N;

    // construct a 2D array say M*N
    cin >> M >> N;

    int * arr =  new int[M*N];

    // input as row major
    return 0;
}
